{
  "name": "lms-project-with-ai",
  "version": "1.0.0",
  "description": "LMS with AI Chatbot",
  "private": true,
  "scripts": {
    "server": "node server.js",
    "dev": "concurrently \"npm run server\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "socket.io": "^4.8.3"
  }
}
